python-telegram-bot[webhooks]==21.0 # Or the very latest 21.x version if 21.0 has issues.
APScheduler==3.10.4
SQLAlchemy==2.0.29 # Keep this as is, as the Python version fixed the error.
httpx==0.27.0
psycopg2-binary==2.9.9